name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm test
      - name: Package VSIX
        run: npx --yes @vscode/vsce package --out sonarqube-dashboard-pipeline.vsix
      - uses: actions/upload-artifact@v4
        with:
          name: sonarqube-dashboard-pipeline-vsix
          path: sonarqube-dashboard-pipeline.vsix
